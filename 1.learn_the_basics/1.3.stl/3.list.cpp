#include <bits/stdc++.h>
using namespace std;


int main() {
    list<int> l;
    l.push_back(11);
    l.emplace_back(2002);

    l.push_front(25);
    l.emplace_front(007);

    // rest functions are same as vector

    return 0;
}
